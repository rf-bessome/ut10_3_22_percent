/////////////////////////////////////////////////////////////////////
//
// Адаптационный модуль. При необходимости здесь можно реализовать 
//  переход на используемую библиотеку.
//
/////////////////////////////////////////////////////////////////////

Процедура ИзменитьВыделенные(ДинамическийСписок) Экспорт
	
	МассивОбъектов = Новый Массив;
	Для Каждого ЭлементОбъект Из ДинамическийСписок.ВыделенныеСтроки Цикл
		МассивОбъектов.Добавить(ЭлементОбъект);
	КонецЦикла;
	Если МассивОбъектов.Количество() = 0 Тогда
		ПоказатьПредупреждениеИС(, НСтр("ru = 'Команда не может быть выполнена для указанного объекта.'"));
		Возврат;
	КонецЕсли;
	
	ГрупповаяОбработкаОбъектов = Обработки.ГрупповаяОбработкаОбъектов.Создать();
	
	МассивТипов = Новый Массив;
	МассивТипов.Добавить(ТипЗнч(МассивОбъектов[0]));
	ДоступныеТипы = Новый ОписаниеТипов(МассивТипов);
	ГрупповаяОбработкаОбъектов.УстановитьДоступныеТипы(ДоступныеТипы);
	
	Для Каждого Элемент Из МассивОбъектов Цикл
		НоваяСтрока = ГрупповаяОбработкаОбъектов.ОбрабатываемыеОбъекты.Добавить();
		НоваяСтрока.Пометка = Истина;
		НоваяСтрока.Объект = Элемент;
	КонецЦикла;
	
	Форма = ГрупповаяОбработкаОбъектов.ПолучитьФорму("Форма");
	Форма.ОткрытьМодально();
	
КонецПроцедуры

