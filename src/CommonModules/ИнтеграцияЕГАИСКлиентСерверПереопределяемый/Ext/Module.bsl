#Область СлужебныйПрограммныйИнтерфейс

// Возвращает имя формы рабочего места для ТТН входящей через параметр.
// 
// Параметры:
//  ИмяФормы - Строка - Исходящий параметр с именем формы.
Процедура ИмяФормыРабочегоМестаПоОформлениюДокументаТТНВходящаяЕГАИС(ИмяФормы) Экспорт
	
	Возврат;
	
КонецПроцедуры

Процедура ПроверитьИспользованиеПодсистемы(Отказ) Экспорт
	ЗначениеКонстанты = ПолучитьФункциональнуюОпцию("ВестиСведенияДляДекларацийПоАлкогольнойПродукции");
	Если Не ЗначениеКонстанты = Истина Тогда
		ТекстСообщения = НСтр("ru = 'Для работы с ЕГАИС необходимо в форме ""Настройка параметров учета""
		|на закладке ""Учет алкоголя"" включить использование данной возможности.'");
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения);
		Отказ = Истина;
	КонецЕсли;
КонецПроцедуры

#КонецОбласти
