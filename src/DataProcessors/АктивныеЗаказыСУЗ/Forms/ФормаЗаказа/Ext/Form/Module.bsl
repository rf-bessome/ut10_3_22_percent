
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
	
	ЗаполнитьЗначенияСвойств(ЭтаФорма, Параметры, "Идентификатор, ДатаЗаказа");
	
	Для каждого Строка из Параметры.СтрокиЗаказа Цикл
		ЗаполнитьЗначенияСвойств(СтрокиЗаказа.Добавить(), Строка);
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

